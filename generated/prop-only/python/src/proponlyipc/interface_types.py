"""
DO NOT MODIFY THIS FILE.  It is automatically generated by Stinger-IPC 0.9.7.post0 and changes will be over-written
on the next generation.

It contains enumerations used by the prop-only interface.

LICENSE: This generated code is not subject to any license restrictions from the generator itself.
TODO: Get license text from stinger file
"""

from pydantic import BaseModel, Field, PlainValidator, PlainSerializer, ConfigDict
from datetime import datetime, timedelta, UTC

from typing import Optional, Union, List
import base64
from enum import IntEnum
from typing import Annotated


def base64_decode_if_str(value: Union[str, bytes, None]) -> Optional[bytes]:
    """If the value is a string, decode it from base64 to bytes.  Otherwise return the bytes as-is."""
    if isinstance(value, str):
        return base64.b64decode(value)
    return value


class PropOnlyInterfaceInfo(BaseModel):
    interface_name: str = Field(default="prop-only")
    title: str = Field(default="Property Only Interface")
    version: str = Field(default="0.0.1")
    instance: str
    connection_topic: str
    timestamp: str
    prefix: str


# There are 1 enum definitions
class Country(IntEnum):
    """Interface enum `country`.
    A small set of supported countries.
    """

    USA = 1
    CANADA = 2
    MEXICO = 3


# There are 1 struct definitions
class Address(BaseModel):
    """Interface struct `address`."""

    model_config = ConfigDict(populate_by_name=True, strict=True, extra="forbid")
    street: Annotated[
        str,
        Field(
            description="The street address.",
        ),
    ]
    city: Annotated[
        str,
        Field(
            description="The city name.",
        ),
    ]
    state: Annotated[
        str,
        Field(
            description="The state or province.",
        ),
    ]
    postal_code: Annotated[
        str,
        Field(
            description="The postal or ZIP code.",
        ),
    ]
    country: Annotated[Country, Field()]


# There are 0 signal definitions


# There are 2 property definitions
class HomeAddressProperty(BaseModel):
    """Interface property `home_address` (multi-value struct).

    The current home address.
    """

    model_config = ConfigDict(populate_by_name=True, strict=True, extra="forbid")
    address: Annotated[Address, Field()]


class FavoriteCountryProperty(BaseModel):
    """Interface property `favorite_country` (multi-value struct).

    The user's favorite country.
    """

    model_config = ConfigDict(populate_by_name=True, strict=True, extra="forbid")
    country: Annotated[Country, Field()]


# There are 0 method definitions
